package algorithm;

public class Result {

	public int solution(String[] lines) {
		int answer=0;
		//1. lines배열 length의 두배 만들어서 시간순으로 정렬된 배열 생성
		for (int i=0; i< lines.length; i++) {
			int result =1;
			for (int j=0; j< lines.length; j++) {
				if (i==j) {
					continue;
				}
				
				else {
					
				}
			}
			
		}
		//2. 배열 full scan 하며 task 시작일 땐 cnt+1, task 종료 시 cnt-1
			
			// if cnt 체크 시점에서 1초 		
		
		return answer;
	}
}
